#include <vector>

// Preenche um vector com um conjunto de índices aleatórios sem repetição no
// intervalo fechado de 0 a size - 1. O tamanho do conjunto gerado é o mesmo
// tamanho do vector de entrada values
void generate_indices(std::vector<int> &values, size_t size);
